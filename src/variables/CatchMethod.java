package variables;

/**
* enum representing different catch methods.
*/
public enum CatchMethod {
    /** first catch method: pole. */
    POLE,
    /** second catch method: net. */
    NET,
    /** third catch method: spear. */
    SPEAR,
    /** last catch method: trap. */
    TRAP,
}
